[project]
name = "analysis"
version = "0.1.0"
description = "Horizon analysis Lambda service"
requires-python = ">=3.13"
dependencies = ["jsonschema>=4.21.0"]

[tool.ruff]
target-version = "py313"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
ignore = []
